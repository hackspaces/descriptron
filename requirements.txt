streamlit==1.25.0  # Ensure to match the installed version
opencv-python==4.5.5.64  # OpenCV for video processing
openai==0.27.8  # OpenAI library for API calls
pillow==9.4.0  # Required by Streamlit for image processing